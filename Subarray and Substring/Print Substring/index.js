
let str='Racecar';
for(let i=0;i<str.length;i++){
    let bag=''
    for(let j=i;j<str.length;j++){
        bag+=str[j]
    }
    console.log(bag)
}

// You can use function also to print the same output
